# Issues

## UI / UX
- [ ] Translucent square in the middle of the screen (grid rendering issue).
- [ ] Polygon tool starts well but never completes a cycle (needs better closing logic).
- [ ] No ground plane.
- [ ] No move tool (or functionality).
- [ ] Highlighting works, but only in play mode (should always work).
- [ ] Box tool should also be a select tool (Box Selection).
- [ ] Spacebar should pause/unpause.
- [ ] Nothing snaps to the grid (and no move tool to test it with).
- [ ] Delete key does not work.
- [ ] Delete via UI causes a warning about invalid entity generation.
- [ ] Grid spacing changes, but grid remains only in the middle of the screen.
- [ ] No camera controller (Pan/Zoom).
- [ ] Drag tool makes a line but doesn't do anything.
- [ ] Inspector tabs should appear on the location of a right click (currently off to the side).

## Planned Fixes
- Fix grid rendering to cover viewport.
- Implement camera controller (Pan/Zoom).
- Fix Polygon tool closing logic (Enter key, larger snap radius).
- Enable physics dragging in Drag Tool.
- Implement Box Selection in Select Tool.
- Implement Move functionality in Select Tool.
- Add Spacebar shortcut for Pause.
- Add Delete key shortcut.
- Add Ground Plane.
