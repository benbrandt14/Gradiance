[package]
name = "gradiance-optimize"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Gradiance layout optimization: the packing/arrangement solver family"

[lints]
workspace = true

[dependencies]
gradiance-core.workspace = true
gradiance-geometry.workspace = true
# Bevy surface is deliberately minimal — `Resource`/`Reflect` derives on the
# configuration struct only (the same allowance the geometry shape tree has).
# No systems, no queries, no plugin lives in this crate.
bevy.workspace = true
argmin.workspace = true
argmin-math.workspace = true
